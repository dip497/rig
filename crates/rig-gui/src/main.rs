//! `rig-gui` — Tauri desktop GUI.
//!
//! M1 deliverable. Visual onboarding, bundle browser, drift diff viewer,
//! unit × agent matrix. Steals UX patterns from `gsd2-config` (3-pane,
//! ⌘K palette, dirty tracking, atomic writes).

fn main() -> anyhow::Result<()> {
    // TODO: tauri::Builder::default().run(...)
    Ok(())
}
